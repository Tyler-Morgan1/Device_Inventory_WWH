extends layout

block content

  h1 #{title}: #{device.serialNumber} : #{device.manufacturer} : #{employee.name}

    p Do you really want to delete this device assignment?

    form(method='POST')
      div.form-group
        input#assigned_deviceid.form-control(type='hidden', name='assigned_deviceid', value=assigned_device._id )

      button.btn.btn-primary(type='submit') Delete